# Core
torch>=2.2.0
efficient-kan>=0.1.0
scipy>=1.12.0
scikit-learn>=1.4.0
mlflow>=2.11.0
pandas>=2.2.0
pyarrow>=15.0.0
pymongo>=4.6.0

# Streaming
confluent-kafka>=2.3.0

# Mock API
fastapi>=0.110.0
uvicorn>=0.29.0

# Dashboard
# streamlit removed — Power BI used instead
# plotly removed — Power BI used instead

# Dev / testing
pytest>=8.0.0
hypothesis>=6.100.0
pytest-cov>=4.0.0

# Optional: local Spark tests
# pyspark>=3.5.0
azure-storage-file-datalake>=12.14.0
azure-identity>=1.16.0
